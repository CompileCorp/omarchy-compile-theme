$color = rgba(8,15,17,1.0)
$inner_color = rgba(8,15,17,1.0)
$outer_color = rgba(72,213,151,1.0)
$font_color = rgba(254,255,255,1.0)
$check_color = rgba(72,213,151,1.0)
#
background {
    monitor =
    path = $HOME/.config/omarchy/themes/felix/backgrounds/wallpaper.png
    blur_passes = 0                  # Optional: Set to 0 for no blur, or higher for blur effect
    blur_size = 8                    # Optional: Size of blur, if enabled
    color = rgba(8, 15, 17, 0.5)    # Optional: Fallback color if image fails
}
